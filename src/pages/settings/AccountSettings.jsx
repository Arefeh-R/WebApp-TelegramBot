import MainCard from 'components/MainCard';
import { Typography } from '@mui/material';

const AccountSettings = () => {
  return (
    <MainCard title="تنظیمات حساب">
      <Typography>در حال توسعه...</Typography>
    </MainCard>
  );
};

export default AccountSettings;